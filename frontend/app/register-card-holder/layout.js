export const metadata = {
    title: "Manage Card Holders",
    description: "Register new card holders and manage existing customer profiles.",
};

export default function CardHolderLayout({ children }) {
    return children;
}
